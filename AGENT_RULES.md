# Agent 工作規則

給 Claude Code 在此專案中遵循的協作規則。每次新對話開始都會透過 CLAUDE.md 自動載入本檔案，不需要使用者每次重複提醒。

## 大改動前必須先出示計畫，取得同意才能動手

**大改動**（需要先出計畫、等使用者同意）：
- 涉及多個檔案、或影響現有資料流（RAG → LLM → agents → guardian → gitops → healer）的改動
- 新增子系統 / 新功能模組（例如新的 agent、新的 API 端點群、新的前端頁面）
- 修改訓練流程（`generate_finetune_data.py`、`clean_dataset.py`、`train_local.py`、`merge_and_train.py`）或訓練資料格式
- 新增/更換套件依賴、修改部署或 CI 相關設定
- 任何刪除資料、重寫既有邏輯、或難以復原的操作（本來就受全域安全規則約束，這裡再次強調）

**小改動**（可直接動手，不用先問）：
- 單一檔案內的 bug 修正、typo、文案調整
- 既有函式的小幅調整（不改變介面/行為範圍）
- 設定值調整、文件更新
- 使用者已經明確講清楚要改什麼、怎麼改的情況（等於已經核准）

大改動時，先用 Plan Mode（EnterPlanMode）整理方案，列出：要改什麼、為什麼、影響範圍、有哪些取捨；等使用者明確同意（ExitPlanMode 核准）後才實際編輯檔案。不要「先斬後奏」。

## 專案階段性目標（會隨進度更新，不是寫死的）

1. **穩定期**：先確保核心資料流（自然語言 → JSON → YAML → agent 審核 → dry-run → 部署 → 監控自癒）能穩定跑，RAG 語意檢索（裝 chromadb）、對話語言一致性等基礎品質問題優先處理。
2. **創意分化期**：核心穩定後，往「跟別人做出區隔」的方向做延伸功能，目前候選方向記錄在對話歷史 / 未來會補進 [docs/roadmap.md](docs/roadmap.md)。
3. 每個階段轉換前，也視為「大改動」，要先跟使用者對齊計畫。

## 其他約定

- 文件與程式碼註解預設用繁體中文，符合現有專案慣例（見 [CLAUDE.md](CLAUDE.md)）。
- 改動前先確認現況（讀程式碼、跑診斷指令），不要憑空假設專案已經有的東西；本檔案內容如與程式碼現況衝突，以程式碼現況為準，並回報使用者更新此檔案。
