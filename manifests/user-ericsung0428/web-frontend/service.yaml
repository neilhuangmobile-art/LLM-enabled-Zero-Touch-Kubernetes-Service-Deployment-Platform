---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-frontend
  name: web-frontend
  namespace: user-ericsung0428
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web-frontend
  type: ClusterIP
