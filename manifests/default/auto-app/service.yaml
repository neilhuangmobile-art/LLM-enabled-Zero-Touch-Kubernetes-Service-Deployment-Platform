---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: auto-app
  name: auto-app
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: auto-app
  type: ClusterIP
