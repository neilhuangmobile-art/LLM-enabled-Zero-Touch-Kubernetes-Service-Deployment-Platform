---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: zt-confirm
  name: zt-confirm
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: zt-confirm
  type: ClusterIP
