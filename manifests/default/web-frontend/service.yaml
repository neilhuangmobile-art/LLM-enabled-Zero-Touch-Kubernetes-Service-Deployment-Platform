---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-frontend
  name: web-frontend
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web-frontend
  type: ClusterIP
