# ── 核心推論 ──────────────────────────────────────────────────
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.27.0

# ── 訓練 ──────────────────────────────────────────────────────
trl>=0.8.0
datasets>=2.18.0

# ── Model Server ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# ── K8s 部署 ──────────────────────────────────────────────────
kubernetes>=29.0.0
pyyaml>=6.0

# ── Web Demo ──────────────────────────────────────────────────
flask>=3.0.0

# ── Claude API（快速推論 + AI 對話，取代本地 GPU）─────────────
anthropic>=0.40.0

# ── 工具 ──────────────────────────────────────────────────────
json_repair>=0.20.0
numpy>=1.24.0

# ── RAG ───────────────────────────────────────────────────────
# scikit-learn：TF-IDF fallback（無 GPU 時自動使用）
scikit-learn>=1.3.0
# sentence-transformers：語意向量（有 GPU 時效果更好，選用）
sentence-transformers>=2.6.0

# ── GitOps（選用，未安裝時自動跳過）──────────────────────────
gitpython>=3.1.0
