# .env.example
# 複製為 .env 並填入實際值，不要 commit .env 到 Git。

# ── Hugging Face ──────────────────────────────────────────────
# 下載 LLaMA-3.1-8B-Instruct 時需要已接受授權協議的 token
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Kubernetes ────────────────────────────────────────────────
# kubeconfig 預設讀 ~/.kube/config，若有自訂路徑填入
KUBECONFIG=~/.kube/config

# ── Model Server ──────────────────────────────────────────────
# 若要更改 model server 監聽位址（預設 127.0.0.1:8765）
MODEL_SERVER_HOST=127.0.0.1
MODEL_SERVER_PORT=8765

# ── GitOps（選用）────────────────────────────────────────────
# Git 倉庫路徑，manifest_writer.py 會在此 commit YAML
GITOPS_REPO_PATH=.

# ── Prometheus（選用）────────────────────────────────────────
# 若有真實 Prometheus，填入 URL
PROMETHEUS_URL=http://localhost:9090

# ── Argo CD（選用）───────────────────────────────────────────
ARGOCD_SERVER=localhost:8080
ARGOCD_TOKEN=
